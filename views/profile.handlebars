<main>
    <h2>Profile</h2>
    <div class="tier">
        <h2>{{name}}</h2>
        <h3>{{profile.name}}</h3>
        <h4>{{profile.services}}</h4>
        <p>{{profile.description}}</p>
        <p>{{profile.price}}</p>
        
    </div>
</main>